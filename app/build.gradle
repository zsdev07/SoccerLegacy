plugins {
    id 'com.android.application'
}

android {
    namespace 'com.soccerlegacy.game'
    compileSdk 34

    defaultConfig {
        applicationId "com.soccerlegacy.game"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "0.01"

        externalNativeBuild {
            cmake {
                cppFlags "-std=c++17"
                arguments "-DANDROID_STL=c++_shared"
            }
        }

        ndk {
            abiFilters 'arm64-v8a', 'x86_64'
        }
    }

    externalNativeBuild {
        cmake {
            // ✅ Relative to app/ folder — NOT repo root
            path "src/main/cpp/CMakeLists.txt"
            version "3.22.1"
        }
    }

    buildTypes {
        debug {
            debuggable true
        }
    }
}
